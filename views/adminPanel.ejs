<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/users.css" rel="stylesheet">

    <title>Users</title>
</head>
<body>
<%= console.log("locals email "+locals.email) %>
    <%- include('partials/_nav', {Email:locals.userName})%>
    <%- include('partials/_adminSide', {Email:locals.userName,ActiveListings: ActiveListingsAmount,userAmount:Users.length,ArchivedListings:closedListingsAmount})%>
<div class="flex justify-content-center mt-4 userslist">
    <ul class="list-group list-group-flush">
        <% for(let i = 0; i<Users.length;i++) {%>
        <li class="list-group-item"><a href="/users/<%=Users[i]._id%>"><%=Users[i].userName%></a>, <%=Users[i].email%> <button class="button btn btn-success">Promote To Admin</button><button class="button btn btn-danger ms-3">Ban User</button></li>
        <%}%>
    </ul>

</div>

<script src="/scripts/users.js"></script>
</body>
</html>
